# Implement a pipeline stage

Implement the pipeline stage described below. Follow the constraints in CLAUDE.md strictly.

- Use the repo structure defined in CLAUDE.md
- All audio at 16kHz mono internally
- Type hints + docstrings on all public functions
- Use module-level logger, no print statements
- Raise specific exceptions, not generic Exception
- Load config from configs/pipeline_config.yaml

Stage to implement: $ARGUMENTS
